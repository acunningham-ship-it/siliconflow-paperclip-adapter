/**
 * Model detection / validation. v0.0.1 stub.
 */
// TODO: implement detectModel() — verify model exists in SiliconFlow catalog
export async function detectModel(): Promise<null> {
  return null;
}
